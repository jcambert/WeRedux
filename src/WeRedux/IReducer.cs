namespace WeRedux
{
    /*  public interface IReducer<TState,TAction>:IDisposable
          where TState:new()
      {
          void Reduce(TState newState, TState state, TAction action);

          void Execute(IActionState<TState, TAction>  action);
      }*/
}
